<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20sp">

    <!--<SearchView
        android:id="@+id/searchBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:iconifiedByDefault="false"
        android:queryHint="Search for games "
        android:fontFamily="@font/roboto"/>-->

    <com.mancj.materialsearchbar.MaterialSearchBar
        android:id="@+id/search_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:mt_placeholder="Search for topics"
        app:mt_hint="search for topics"/>

    <TextView
        android:id="@+id/GameTopicsList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/search_bar"
        android:layout_marginTop="20sp"
        android:layout_weight="1"
        android:fontFamily="@font/roboto"
        android:text="Game topic list "
        android:textSize="20sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewTopics"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/GameTopicsList"
        android:layout_marginTop="20sp"
        android:orientation="horizontal"/>

    <ImageButton
        android:id="@+id/addTopic"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentRight="true"
        android:background="@drawable/button_border3"
        android:src="@drawable/ic_add"
        android:layout_alignParentBottom="true"/>


</RelativeLayout>